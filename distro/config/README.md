# Distro config

- **packages.txt** – Extra packages for the live system (Node 22+ via NodeSource or PPA should be added in scripts or here).
- **live-build/** – Optional: live-build config (e.g. `config/`, `chroot.hooks/`) when using `lb build` instead of Cubic.
